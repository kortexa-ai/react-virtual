import QueryProvider from "./QueryProvider"
import MainContent from "./MainContent"

function App() {
  return (
    <QueryProvider>
      <MainContent />
    </QueryProvider>
  )
}

export default App
